export interface IPaths {
  /**
   * Src dir
   */
  SRC: string;
  /**
   * Themes dir
   */
  THEMES: string;
  /**
   * Extension directory
   */
  VSIX_DIR: string;
  VSIX_SRC_DIR: string;
  /**
   * Internal Extensions directory
   */
  EXT_DIR: string;
  /**
   * UI directory
   */
  UI: string;
}
